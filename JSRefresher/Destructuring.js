// We use destructoring to extract array element or object property and store them in variable

const numbers = [1,2,3];

// We extract first and sacond element from array
[n1,n2] = numbers;

console.log(n1,n2)