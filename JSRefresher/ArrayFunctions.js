const numbers = [1,2,3];

// This map will go through every element and we can manipulate with each element in array
const doubleNumArray = numbers.map((num) =>{
    return num * 2;
})

console.log(doubleNumArray)